defaults:
  - /datamodule/base/crowd@crowd

crowd:
  knp_path: ${dataset.path}/valid/crowd
  jsonl_path: ${dataset.path}/valid/jsonl
  gold_knp_path: ${dataset.path}/valid/crowd_gold
  training: false
